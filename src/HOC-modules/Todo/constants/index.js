export const TODO_CREATE_REQUESTED = (name) =>
  `${name}/TODO/CREATE_REQUESTED`
export const TODO_CREATE_SUCCEEDED = (name) =>
  `${name}/TODO/CREATE_SUCCEEDED`
export const TODO_CREATE_FAILED = (name) =>
  `${name}/TODO/CREATE_FAILED`

export const TODOS_GET_REQUESTED = (name) =>
  `${name}/TODOS/GET_REQUESTED`
export const TODOS_GET_SUCCEEDED = (name) =>
  `${name}/TODOS/GET_SUCCEEDED`
export const TODOS_GET_FAILED = (name) =>
  `${name}/TODOS/GET_FAILED`

export const TODO_UPDATE_REQUESTED = (name) =>
  `${name}/TODO/UPDATE_REQUESTED`
export const TODO_UPDATE_SUCCEEDED = (name) =>
  `${name}/TODO/UPDATE_SUCCEEDED`
export const TODO_UPDATE_FAILED = (name) =>
  `${name}/TODO/UPDATE_FAILED`

export const TODO_DELETE_REQUESTED = (name) =>
  `${name}/TODO/DELETE_REQUESTED`
export const TODO_DELETE_SUCCEEDED = (name) =>
  `${name}/TODO/DELETE_SUCCEEDED`
export const TODO_DELETE_FAILED = (name) =>
  `${name}/TODO/DELETE_FAILED`
